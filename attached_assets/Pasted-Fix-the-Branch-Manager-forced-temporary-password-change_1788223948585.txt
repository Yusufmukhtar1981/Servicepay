Fix the Branch Manager forced temporary-password change flow completely.

CURRENT BUG:
When a newly created Branch Manager logs in successfully with the temporary password, they are redirected to the “Change temporary password” screen.

The screen currently shows only:
1. New password
2. Confirm new password

But when Continue is clicked, the backend returns:
“Current password, new password and confirmation are required.”

This means the backend requires the temporary/current password, but the UI has no field for entering it.

FIX THIS END-TO-END:

1. On the Branch Manager “Change temporary password” screen, add THREE password fields in this exact order:

   - Temporary password
     Placeholder/label: “Temporary password”
     This is the temporary password the Branch Manager used to log in.

   - New password
     Label: “New password”

   - Confirm new password
     Label: “Confirm new password”

2. All password fields must:
   - have show/hide eye icons
   - be properly spaced with no overlapping labels
   - match the existing ServicePay premium green UI
   - work properly on mobile and desktop

3. When Continue is clicked, send ALL THREE required values to the backend using the exact property names expected by the existing change-password endpoint.

IMPORTANT:
Inspect the backend route/controller first and use its actual expected field names.

For example, if backend expects:
{
  currentPassword,
  newPassword,
  confirmPassword
}
then send exactly:
{
  currentPassword: temporaryPasswordInput,
  newPassword: newPasswordInput,
  confirmPassword: confirmPasswordInput
}

Do not invent different payload names if the backend already has established names.

4. Validate before API request:
   - Temporary password cannot be empty
   - New password cannot be empty
   - Confirm password cannot be empty
   - New password and confirmation must match
   - Enforce the existing ServicePay password security requirements

5. After successful password change:
   - update the Branch Manager so temporary-password/force-password-change status becomes false
   - invalidate the temporary password as appropriate
   - preserve/login the Branch Manager with the new password where supported
   - redirect directly to the Branch Manager Dashboard
   - show success message: “Password changed successfully”

6. After this is completed, the Branch Manager must be able to log out and log back in using the NEW password.

7. The OLD temporary password must no longer be accepted after the password has been successfully changed.

8. Handle API errors clearly instead of silently failing.

9. Do NOT affect:
   - Customer login
   - Admin login
   - Staff login
   - Rider login
   - Business Partner login
   - Existing Branch Managers who already changed their passwords

10. Test the complete flow:
Admin creates new Branch Manager
→ temporary login credentials are generated
→ Branch Manager logs in with temporary password
→ forced Change Temporary Password screen appears
→ enters Temporary Password
→ enters New Password
→ confirms New Password
→ Continue
→ password changes successfully
→ redirected to Branch Manager Dashboard
→ logout
→ login again with new password successfully
→ old temporary password is rejected.

Also fix the current UI overlap visible around the Confirm New Password field.

Do not stop after changing only the frontend. Verify both frontend and backend and complete the full flow before reporting success.